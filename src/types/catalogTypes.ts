export interface IFilterFormValues {
    title: string ;
    minPrice: string ;
    maxPrice: string ;
}
export interface ILaptopProps {
    title: string;
    price: number | null;
    img: string;
}